<h1>id: <%= @task.id %> のTask編集ページ</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'メッセージ' %>
  <%= f.text_field :content %>

  <%= f.submit 'Task投稿' %>
<% end %>

<%= link_to 'Task一覧に戻る', tasks_path %>