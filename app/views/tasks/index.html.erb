<h1>Task一覧 (Status) </h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.content %> ( <%= task.status %> )</li>
  <% end %>
</ul>

<%= link_to '新規Task投稿', new_task_path %>
